# --- Frame-Distill Dependencies ---

# Computer Vision & Math Engine (For Role 1)
numpy==1.26.4
opencv-python==4.9.0.80
scikit-image==0.22.0

# Terminal UI & Reporting
rich==13.7.1          # Beautiful terminal output and progress bars
Jinja2==3.1.3         # HTML template rendering for final report

# Deep Learning (Anomaly Classification)
torch
torchvision
